<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <AssemblyName>Hawkynt.ProcessManager.Platform.Linux</AssemblyName>
    <RootNamespace>Hawkynt.ProcessManager.Platform.Linux</RootNamespace>
    <Version>0.1.0</Version>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\ProcessManager.Core\ProcessManager.Core.csproj" />
  </ItemGroup>

  <!-- The parsers are internal and are tested directly against recorded text, the same way the
       Windows project exposes its bulk-query walk (PRD 9.4). -->
  <ItemGroup>
    <InternalsVisibleTo Include="Hawkynt.ProcessManager.Tests" />
  </ItemGroup>

</Project>
